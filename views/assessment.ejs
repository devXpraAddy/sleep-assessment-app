<!DOCTYPE html>
<html>
  <head>
    <title>Sleep Assessment</title>
    <!-- Bootstrap CSS and Custom CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" type="text/css" href="/style.css" />
  </head>
  <body>
    <div class="container mt-5">
      <h2 class="text-center">Sleep Assessment</h2>
      <form
        action="/sleep-assessment/<%= sessionId %>/answers"
        method="POST"
        class="mt-4"
      >
        <!-- Question 1 -->
        <div class="mb-3">
          <label class="form-label"
            >1. How long have you been struggling with sleep?</label
          >
          <select name="answers[q1][answer]" class="form-select">
            <option value="Less than 2 weeks">Less than 2 weeks</option>
            <option value="2 to 8 weeks">2 to 8 weeks</option>
            <option value="More than 8 weeks">More than 8 weeks</option>
          </select>
          <input type="hidden" name="answers[q1][questionId]" value="q1" />
        </div>

        <!-- Question 2 -->
        <div class="mb-3">
          <label class="form-label"
            >2. What time do you usually go to bed for sleep?</label
          >
          <input
            type="time"
            name="answers[q2][answer]"
            class="form-control"
            required
          />
          <input type="hidden" name="answers[q2][questionId]" value="q2" />
        </div>

        <!-- Question 3 -->
        <div class="mb-3">
          <label class="form-label"
            >3. What time do you get out of bed to start your day?</label
          >
          <input
            type="time"
            name="answers[q3][answer]"
            class="form-control"
            required
          />
          <input type="hidden" name="answers[q3][questionId]" value="q3" />
        </div>

        <!-- Question 4 -->
        <div class="mb-3">
          <label class="form-label"
            >4. How many hours of sleep do you get in a typical night?</label
          >
          <input
            type="number"
            name="answers[q4][answer]"
            class="form-control"
            min="0"
            max="24"
            required
          />
          <input type="hidden" name="answers[q4][questionId]" value="q4" />
        </div>

        <button type="submit" class="btn btn-primary w-100">Submit</button>
      </form>
    </div>

    <!-- Bootstrap JS (Optional if using interactive components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
